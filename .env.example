# Copy to .env on the machine that runs the MCP server.
# Do not commit the real .env file.

ORACLE_MCP_CONFIG=profiles.yaml

DEV_ORACLE_USER=readonly_user
DEV_ORACLE_PASSWORD=change-me

TEST_ORACLE_USER=readonly_user
TEST_ORACLE_PASSWORD=change-me
